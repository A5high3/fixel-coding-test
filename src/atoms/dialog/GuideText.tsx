import DialogContentText from "@mui/material/DialogContentText";

export default function DialogGuideText(props: { titleText: string }) {
  return <DialogContentText>{props.titleText}</DialogContentText>;
}
